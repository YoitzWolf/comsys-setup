

[build]
target = "index.html"
dist = "../dist"

tls_key_path  = "keys/server.key"
tls_cert_path = "keys/server.pem"

[serve]
# The address to serve on LAN.
address = "127.0.0.1"
# The address to serve on WAN.
# address = "0.0.0.0"
# The port to serve on.
port = 88

# TLS
